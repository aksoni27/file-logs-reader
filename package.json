{
  "name": "nektar-assignment",
  "version": "1.0.0",
  "description": "read logfile based on timestamp and return to the client",
  "main": "server/server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  }
}
